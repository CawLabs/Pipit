# features/example/

Feature module: UI + hooks + types colocated per domain.

## Wired demo

- ExampleFeature.tsx calls Rust via src/lib/tauri.ts
- Command greet_example in src-tauri/src/commands/example.rs